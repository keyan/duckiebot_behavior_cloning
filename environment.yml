name: behavior_clone
channels:
  - pytorch
  - defaults
dependencies:
  - scikit-learn
  - pytorch
  - torchvision
  - opencv
  - numpy
  - tqdm
  - pillow
  - tensorboard
prefix: /opt/miniconda3/envs/behavior_clone
